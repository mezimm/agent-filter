# /run is wiped at every boot; the socket directory must be recreated before
# the socket units bind. Mode 0751: agent and proxy can traverse to their
# sockets but cannot list the directory.
d /run/approval-broker 0751 broker broker -
